import 'package:flutter/cupertino.dart';

class TweenAnimationPage extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return const Column(children: [Flexible(child: Text("Tween Animation"))],);
  }
}